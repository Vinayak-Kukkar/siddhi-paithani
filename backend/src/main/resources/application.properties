spring.application.name=siddhi-paithani
spring.threads.virtual.enabled=true

# =========================================================
# 🗄️ DATABASE CONFIGURATION (MySQL / PostgreSQL / H2)
# =========================================================
spring.datasource.url=${DB_URL:${DATABASE_URL:jdbc:mysql://localhost:3306/siddhi_paithani?allowPublicKeyRetrieval=true&useSSL=false}}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:7219120935}

# Auto-generate & update schema definitions from @Entity classes
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

spring.thymeleaf.cache=false
server.port=${PORT:8084}

# =========================================================
# 💰 REAL UPI / QR CODE PAYMENT CONFIGURATION
# =========================================================
upi.id=${UPI_ID:7219120935@ybl}
upi.name=${UPI_NAME:Siddhi Paithani}

# =========================================================
# 📧 REAL EMAIL CONFIGURATION (Gmail SMTP)
# =========================================================
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:kukkarvinayak11@gmail.com}
spring.mail.password=${MAIL_PASSWORD:eapswgkoxldtwuay}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# =========================================================
# 📱 REAL SMS GATEWAY CONFIGURATION (Fast2SMS / Twilio)
# =========================================================
sms.gateway.api-key=${SMS_API_KEY:VwfqKlPM6WekSR5mgr492sjLt8xY0y7XBJGNUCzT3Ip1nOoHah2CAZBHncmN34htVFrbw9zuRUIL7Ddq}
sms.gateway.url=${SMS_GATEWAY_URL:https://www.fast2sms.com/dev/bulkV2}

# =========================================================
# 🔒 ADMIN PORTAL SECURITY CREDENTIALS
# =========================================================
admin.username=${ADMIN_USERNAME:Vinuuu}
admin.password=${ADMIN_PASSWORD:Anvita@2404}